### TS Utilities

These files are slightly modified versions of those in the `builder-scaffold` repository, customized for our local deployment environment.

They are used for local development and testing and are not used in normal FTA operation.